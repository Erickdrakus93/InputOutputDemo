const express = require('express');

 function createRoter(db){
   const router = express.Router();
   const owner = '';

   //Here we define the routers Here
   return router;
 }

 module.exports = createRoter;
 
